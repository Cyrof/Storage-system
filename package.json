{
  "name": "storage-system",
  "version": "1.0.0",
  "description": "—-Instructions—-\r # Download file either clone to repo or download via zip file. \r # Once activate virtual environment to start running server. Type the following script into terminal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Cyrof",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.1.3"
  }
}
